u8   → will live anywhere
u16  → only even numbers
u32  → only multiples of 4
u64  → only multiples of 8

1. tag/flags: u8     ← identifiers first
2. other u8 fields   ← pack small fields together
3. u16 fields        ← fits after u8s naturally
4. padding: [u8; N]  ← pad to reach multiple of 8
5. u64/i64 fields    ← largest alignment first
6. u32 fields        ← then medium
7. [u8; N] arrays    ← always last, never causes alignment issues

#[repr(C)]
struct GoodOrder {
    big: u64,     // 8 bytes
    medium: u16,  // 2 bytes  
    small: u8,    // 1 byte
    // padding: [u8; 5] since the struct size needs to be aligned to 8 bytes.
}



The golden rule is to order struct fields from largest to smallest alignment requirements to minimize padding and reduce memory usage

-------

1. Complete orderbook_program
   → all instructions working
   → place_order fills MatchResults
   → CPI calls stubbed out / skipped for now

2. Build risk_program
   → all state + instructions
   → settle_fill accepts FillEvent
   → defines exact account interface

3. Wire CPI
   → orderbook calls risk_program::settle_fill
   → claim_fill calls risk_program::settle_fill
   → both programs compiled together
   → integration test end-to-end

How To Stub The CPI For Now
In place_order and claim_fill, just skip the CPI call while building:
rust// TODO: CPI to risk_program::settle_fill
// Skipping until risk_program is built
// fill data is in result.fills[i]
Or log it so you can verify fills are correct:
rust// Temporary — verify fill data is correct
for i in 0..result.fill_count as usize {
    let fill = &result.fills[i];
    // just verify fields are populated correctly
    // CPI added in phase 3
}
```

---

## Your Build Plan
```
PHASE 1 — orderbook_program (now)
  ✅ create_market
  → create_open_orders_account
  → place_order             (CPI stubbed)
  → cancel_order
  → claim_fill              (CPI stubbed)
  → cancel_all_orders
  → cancel_order_by_client_id
  → edit_order
  → place_take_order
  → prune_orders

PHASE 2 — risk_program
  → all state accounts
  → deposit / withdraw
  → open_position / close_position
  → settle_fill             ← defines CPI interface
  → funding
  → liquidation
  → insurance

PHASE 3 — CPI wiring
  → orderbook place_order   → risk_program settle_fill
  → orderbook claim_fill    → risk_program settle_fill
  → integration tests
  
  ---
  