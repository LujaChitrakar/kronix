/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ORDERBOOK_PROGRAM_ADDRESS } from "../programs";

export const SET_DELEGATE_DISCRIMINATOR = 11;

export function getSetDelegateDiscriminatorBytes() {
  return getU8Encoder().encode(SET_DELEGATE_DISCRIMINATOR);
}

export type SetDelegateInstruction<
  TProgram extends string = typeof ORDERBOOK_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountOpenOrdersAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountOpenOrdersAccount extends string
        ? WritableAccount<TAccountOpenOrdersAccount>
        : TAccountOpenOrdersAccount,
      ...TRemainingAccounts,
    ]
  >;

export type SetDelegateInstructionData = {
  discriminator: number;
  delegate: ReadonlyUint8Array;
};

export type SetDelegateInstructionDataArgs = { delegate: ReadonlyUint8Array };

export function getSetDelegateInstructionDataEncoder(): FixedSizeEncoder<SetDelegateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["delegate", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: SET_DELEGATE_DISCRIMINATOR }),
  );
}

export function getSetDelegateInstructionDataDecoder(): FixedSizeDecoder<SetDelegateInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["delegate", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getSetDelegateInstructionDataCodec(): FixedSizeCodec<
  SetDelegateInstructionDataArgs,
  SetDelegateInstructionData
> {
  return combineCodec(
    getSetDelegateInstructionDataEncoder(),
    getSetDelegateInstructionDataDecoder(),
  );
}

export type SetDelegateInput<
  TAccountSigner extends string = string,
  TAccountOpenOrdersAccount extends string = string,
> = {
  /** signer */
  signer: TransactionSigner<TAccountSigner>;
  /** OO account */
  openOrdersAccount: Address<TAccountOpenOrdersAccount>;
  delegate: SetDelegateInstructionDataArgs["delegate"];
};

export function getSetDelegateInstruction<
  TAccountSigner extends string,
  TAccountOpenOrdersAccount extends string,
  TProgramAddress extends Address = typeof ORDERBOOK_PROGRAM_ADDRESS,
>(
  input: SetDelegateInput<TAccountSigner, TAccountOpenOrdersAccount>,
  config?: { programAddress?: TProgramAddress },
): SetDelegateInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountOpenOrdersAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? ORDERBOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    openOrdersAccount: {
      value: input.openOrdersAccount ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("openOrdersAccount", accounts.openOrdersAccount),
    ],
    data: getSetDelegateInstructionDataEncoder().encode(
      args as SetDelegateInstructionDataArgs,
    ),
    programAddress,
  } as SetDelegateInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountOpenOrdersAccount
  >);
}

export type ParsedSetDelegateInstruction<
  TProgram extends string = typeof ORDERBOOK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** signer */
    signer: TAccountMetas[0];
    /** OO account */
    openOrdersAccount: TAccountMetas[1];
  };
  data: SetDelegateInstructionData;
};

export function parseSetDelegateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetDelegateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { signer: getNextAccount(), openOrdersAccount: getNextAccount() },
    data: getSetDelegateInstructionDataDecoder().decode(instruction.data),
  };
}
