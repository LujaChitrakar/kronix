/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { RISK_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_INSURANCE_FUND_DISCRIMINATOR = 1;

export function getInitializeInsuranceFundDiscriminatorBytes() {
  return getU8Encoder().encode(INITIALIZE_INSURANCE_FUND_DISCRIMINATOR);
}

export type InitializeInsuranceFundInstruction<
  TProgram extends string = typeof RISK_PROGRAM_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountInsuranceFund extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountInsuranceFund extends string
        ? WritableAccount<TAccountInsuranceFund>
        : TAccountInsuranceFund,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeInsuranceFundInstructionData = {
  discriminator: number;
  bump: number;
  padding: ReadonlyUint8Array;
};

export type InitializeInsuranceFundInstructionDataArgs = {
  bump: number;
  padding: ReadonlyUint8Array;
};

export function getInitializeInsuranceFundInstructionDataEncoder(): FixedSizeEncoder<InitializeInsuranceFundInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["padding", fixEncoderSize(getBytesEncoder(), 7)],
    ]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_INSURANCE_FUND_DISCRIMINATOR,
    }),
  );
}

export function getInitializeInsuranceFundInstructionDataDecoder(): FixedSizeDecoder<InitializeInsuranceFundInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["padding", fixDecoderSize(getBytesDecoder(), 7)],
  ]);
}

export function getInitializeInsuranceFundInstructionDataCodec(): FixedSizeCodec<
  InitializeInsuranceFundInstructionDataArgs,
  InitializeInsuranceFundInstructionData
> {
  return combineCodec(
    getInitializeInsuranceFundInstructionDataEncoder(),
    getInitializeInsuranceFundInstructionDataDecoder(),
  );
}

export type InitializeInsuranceFundInput<
  TAccountPayer extends string = string,
  TAccountInsuranceFund extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Fee payer */
  payer: TransactionSigner<TAccountPayer>;
  /** InsuranceFund PDA */
  insuranceFund: Address<TAccountInsuranceFund>;
  /** System program */
  systemProgram?: Address<TAccountSystemProgram>;
  bump: InitializeInsuranceFundInstructionDataArgs["bump"];
  padding: InitializeInsuranceFundInstructionDataArgs["padding"];
};

export function getInitializeInsuranceFundInstruction<
  TAccountPayer extends string,
  TAccountInsuranceFund extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RISK_PROGRAM_PROGRAM_ADDRESS,
>(
  input: InitializeInsuranceFundInput<
    TAccountPayer,
    TAccountInsuranceFund,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeInsuranceFundInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountInsuranceFund,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? RISK_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    insuranceFund: { value: input.insuranceFund ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("insuranceFund", accounts.insuranceFund),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeInsuranceFundInstructionDataEncoder().encode(
      args as InitializeInsuranceFundInstructionDataArgs,
    ),
    programAddress,
  } as InitializeInsuranceFundInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountInsuranceFund,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeInsuranceFundInstruction<
  TProgram extends string = typeof RISK_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Fee payer */
    payer: TAccountMetas[0];
    /** InsuranceFund PDA */
    insuranceFund: TAccountMetas[1];
    /** System program */
    systemProgram: TAccountMetas[2];
  };
  data: InitializeInsuranceFundInstructionData;
};

export function parseInitializeInsuranceFundInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeInsuranceFundInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      insuranceFund: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeInsuranceFundInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
