/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { RISK_PROGRAM_PROGRAM_ADDRESS } from "../programs";
import {
  getOrderMarginParamsDecoder,
  getOrderMarginParamsEncoder,
  type OrderMarginParams,
  type OrderMarginParamsArgs,
} from "../types";

export const RELEASE_ORDER_MARGIN_DISCRIMINATOR = 16;

export function getReleaseOrderMarginDiscriminatorBytes() {
  return getU8Encoder().encode(RELEASE_ORDER_MARGIN_DISCRIMINATOR);
}

export type ReleaseOrderMarginInstruction<
  TProgram extends string = typeof RISK_PROGRAM_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountUserAccount extends string | AccountMeta<string> = string,
  TAccountMarketConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountUserAccount extends string
        ? WritableAccount<TAccountUserAccount>
        : TAccountUserAccount,
      TAccountMarketConfig extends string
        ? ReadonlyAccount<TAccountMarketConfig>
        : TAccountMarketConfig,
      ...TRemainingAccounts,
    ]
  >;

export type ReleaseOrderMarginInstructionData = {
  discriminator: number;
  orderMarginParams: OrderMarginParams;
};

export type ReleaseOrderMarginInstructionDataArgs = {
  orderMarginParams: OrderMarginParamsArgs;
};

export function getReleaseOrderMarginInstructionDataEncoder(): FixedSizeEncoder<ReleaseOrderMarginInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["orderMarginParams", getOrderMarginParamsEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: RELEASE_ORDER_MARGIN_DISCRIMINATOR,
    }),
  );
}

export function getReleaseOrderMarginInstructionDataDecoder(): FixedSizeDecoder<ReleaseOrderMarginInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["orderMarginParams", getOrderMarginParamsDecoder()],
  ]);
}

export function getReleaseOrderMarginInstructionDataCodec(): FixedSizeCodec<
  ReleaseOrderMarginInstructionDataArgs,
  ReleaseOrderMarginInstructionData
> {
  return combineCodec(
    getReleaseOrderMarginInstructionDataEncoder(),
    getReleaseOrderMarginInstructionDataDecoder(),
  );
}

export type ReleaseOrderMarginInput<
  TAccountSigner extends string = string,
  TAccountUserAccount extends string = string,
  TAccountMarketConfig extends string = string,
> = {
  /** Order owner */
  signer: TransactionSigner<TAccountSigner>;
  /** UserAccount PDA */
  userAccount: Address<TAccountUserAccount>;
  /** MarketConfig */
  marketConfig: Address<TAccountMarketConfig>;
  orderMarginParams: ReleaseOrderMarginInstructionDataArgs["orderMarginParams"];
};

export function getReleaseOrderMarginInstruction<
  TAccountSigner extends string,
  TAccountUserAccount extends string,
  TAccountMarketConfig extends string,
  TProgramAddress extends Address = typeof RISK_PROGRAM_PROGRAM_ADDRESS,
>(
  input: ReleaseOrderMarginInput<
    TAccountSigner,
    TAccountUserAccount,
    TAccountMarketConfig
  >,
  config?: { programAddress?: TProgramAddress },
): ReleaseOrderMarginInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountUserAccount,
  TAccountMarketConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? RISK_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    userAccount: { value: input.userAccount ?? null, isWritable: true },
    marketConfig: { value: input.marketConfig ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("userAccount", accounts.userAccount),
      getAccountMeta("marketConfig", accounts.marketConfig),
    ],
    data: getReleaseOrderMarginInstructionDataEncoder().encode(
      args as ReleaseOrderMarginInstructionDataArgs,
    ),
    programAddress,
  } as ReleaseOrderMarginInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountUserAccount,
    TAccountMarketConfig
  >);
}

export type ParsedReleaseOrderMarginInstruction<
  TProgram extends string = typeof RISK_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Order owner */
    signer: TAccountMetas[0];
    /** UserAccount PDA */
    userAccount: TAccountMetas[1];
    /** MarketConfig */
    marketConfig: TAccountMetas[2];
  };
  data: ReleaseOrderMarginInstructionData;
};

export function parseReleaseOrderMarginInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedReleaseOrderMarginInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      userAccount: getNextAccount(),
      marketConfig: getNextAccount(),
    },
    data: getReleaseOrderMarginInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
