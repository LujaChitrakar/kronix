/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { RISK_PROGRAM_PROGRAM_ADDRESS } from "../programs";
import {
  getOrderMarginParamsDecoder,
  getOrderMarginParamsEncoder,
  type OrderMarginParams,
  type OrderMarginParamsArgs,
} from "../types";

export const RESERVE_ORDER_MARGIN_DISCRIMINATOR = 15;

export function getReserveOrderMarginDiscriminatorBytes() {
  return getU8Encoder().encode(RESERVE_ORDER_MARGIN_DISCRIMINATOR);
}

export type ReserveOrderMarginInstruction<
  TProgram extends string = typeof RISK_PROGRAM_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountUserAccount extends string | AccountMeta<string> = string,
  TAccountMarketConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountUserAccount extends string
        ? WritableAccount<TAccountUserAccount>
        : TAccountUserAccount,
      TAccountMarketConfig extends string
        ? ReadonlyAccount<TAccountMarketConfig>
        : TAccountMarketConfig,
      ...TRemainingAccounts,
    ]
  >;

export type ReserveOrderMarginInstructionData = {
  discriminator: number;
  orderMarginParams: OrderMarginParams;
};

export type ReserveOrderMarginInstructionDataArgs = {
  orderMarginParams: OrderMarginParamsArgs;
};

export function getReserveOrderMarginInstructionDataEncoder(): FixedSizeEncoder<ReserveOrderMarginInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["orderMarginParams", getOrderMarginParamsEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: RESERVE_ORDER_MARGIN_DISCRIMINATOR,
    }),
  );
}

export function getReserveOrderMarginInstructionDataDecoder(): FixedSizeDecoder<ReserveOrderMarginInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["orderMarginParams", getOrderMarginParamsDecoder()],
  ]);
}

export function getReserveOrderMarginInstructionDataCodec(): FixedSizeCodec<
  ReserveOrderMarginInstructionDataArgs,
  ReserveOrderMarginInstructionData
> {
  return combineCodec(
    getReserveOrderMarginInstructionDataEncoder(),
    getReserveOrderMarginInstructionDataDecoder(),
  );
}

export type ReserveOrderMarginInput<
  TAccountSigner extends string = string,
  TAccountUserAccount extends string = string,
  TAccountMarketConfig extends string = string,
> = {
  /** Order owner */
  signer: TransactionSigner<TAccountSigner>;
  /** UserAccount PDA */
  userAccount: Address<TAccountUserAccount>;
  /** MarketConfig */
  marketConfig: Address<TAccountMarketConfig>;
  orderMarginParams: ReserveOrderMarginInstructionDataArgs["orderMarginParams"];
};

export function getReserveOrderMarginInstruction<
  TAccountSigner extends string,
  TAccountUserAccount extends string,
  TAccountMarketConfig extends string,
  TProgramAddress extends Address = typeof RISK_PROGRAM_PROGRAM_ADDRESS,
>(
  input: ReserveOrderMarginInput<
    TAccountSigner,
    TAccountUserAccount,
    TAccountMarketConfig
  >,
  config?: { programAddress?: TProgramAddress },
): ReserveOrderMarginInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountUserAccount,
  TAccountMarketConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? RISK_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    userAccount: { value: input.userAccount ?? null, isWritable: true },
    marketConfig: { value: input.marketConfig ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("userAccount", accounts.userAccount),
      getAccountMeta("marketConfig", accounts.marketConfig),
    ],
    data: getReserveOrderMarginInstructionDataEncoder().encode(
      args as ReserveOrderMarginInstructionDataArgs,
    ),
    programAddress,
  } as ReserveOrderMarginInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountUserAccount,
    TAccountMarketConfig
  >);
}

export type ParsedReserveOrderMarginInstruction<
  TProgram extends string = typeof RISK_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Order owner */
    signer: TAccountMetas[0];
    /** UserAccount PDA */
    userAccount: TAccountMetas[1];
    /** MarketConfig */
    marketConfig: TAccountMetas[2];
  };
  data: ReserveOrderMarginInstructionData;
};

export function parseReserveOrderMarginInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedReserveOrderMarginInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      userAccount: getNextAccount(),
      marketConfig: getNextAccount(),
    },
    data: getReserveOrderMarginInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
